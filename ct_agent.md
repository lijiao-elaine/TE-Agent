初始化页面
渲染参数输入区
   用户点“开始生成”
校验参数
记录输出目录状态（before）
构造命令
执行命令（run cmd）
记录日志 / 状态 / 时间
扫描新增目录（after - before）
分析每个目录（CMakeLists + cpp）
保存会话状态
渲染输出结果
每个文件用 render_editable_file 编辑
    ├ 保存 → 写到磁盘、历史++
    ├ 撤销 → 历史回退
    └ 重置 → 回到最初版本
